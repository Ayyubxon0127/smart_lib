name: smart_kutubxona
description: Universitet Kutubxona Tizimi
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.12

  # State management
  provider: ^6.1.1

  # UI
  cached_network_image: ^3.3.1
  intl: ^0.18.1
  google_fonts: ^6.1.0

  # Utils
  uuid: ^4.2.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
